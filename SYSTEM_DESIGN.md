# NFT Creator Matching Platform - System Design

## 1. アーキテクチャ概要
本システムは、中央集権的な決済サーバー（Stripe等）を持たず、ブロックチェーン上のスマートコントラクトがエスクロー（一時預かり）と分配を行うWeb3 DApp（分散型アプリケーション）です。

### 登場人物
1. **クリエイター**: 作品を作成し、納品する。
2. **クライアント**: 仮想通貨を支払い、作品を依頼する。
3. **プラットフォーム（SK）**: マッチングの場を提供し、スマートコントラクト経由で手数料を自動徴収する。

## 2. コアワークフロー（スマートコントラクトの挙動）
1. **リクエスト（依頼）**:
   - クライアントがクリエイターに依頼を送信する際、スマートコントラクトに報酬額（例: 100 MATIC）をデポジット（預託）する。
   - 資金はコントラクト内にロックされるため、持ち逃げを防ぐ（エスクロー機能）。
2. **納品とNFT化 (Mint)**:
   - クリエイターが作品をIPFSにアップロードし、コントラクトに納品トランザクションを送る。
   - コントラクトが作品をNFTとしてミントし、クライアントのウォレットへ送付する。
3. **報酬の自動分配 (Split Payment)**:
   - 納品と同時に、ロックされていた資金が解除される。
   - 例: 90 MATIC がクリエイターへ、10 MATIC (プラットフォーム手数料10%) がSKの運営ウォレットへ一瞬で自動送金される。

## 3. ディレクトリ構成予定
- `contracts/`: Solidityスマートコントラクト
- `frontend/`: Next.js DAppフロントエンド
- `scripts/`: デプロイ用スクリプト
